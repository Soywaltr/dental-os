// src/components/vistas/Dashboard.jsx
// En blanco a propósito, por pedido explícito del usuario: se va a reconstruir
// desde cero en un paso siguiente. No queda lógica ni estilos de la versión
// anterior -- un punto de partida limpio, no una versión oculta/comentada.
export default function Dashboard() {
  return null;
}
